# ========================================
# ASR Worker Python 依赖
# ========================================
# 核心：PyTorch + CUDA
# 安装命令（Windows + CUDA 12.x）：
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# CPU 版（无 GPU）：
#   pip install torch torchaudio
# ========================================

torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.40.0
huggingface_hub>=0.23.0
soundfile>=0.12.0
numpy>=1.24.0
